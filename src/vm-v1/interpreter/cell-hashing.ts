// Hashing of the memory cells.
//   Hashing is critical to letting the memory layout identify identical cells.

import { MemoryCell } from "../../vm-api/memory-store";

export function createMemoryCellHash(cell: MemoryCell): string {
    return "<not implemented>"
}
