// Locations of the source.

export type { SourcePosition, RuntimeSourcePosition } from './position'
export { createCoreSource } from './core'

// internal is not exported explicitly, because it's for internal use.
